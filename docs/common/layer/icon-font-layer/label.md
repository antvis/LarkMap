`TextLayerOptions` optional

标签标注。

#### `label.`field

`string` optional

标签值映射关联字段。

#### `label.`visible

`boolean` optional default: `true`

标签是否可见。

#### `label.`style

标签样式详细配置见 [TextLayerStyleOptions](https://l7plot.antv.vision/zh/docs/api/composite-layers/text-layer#code-classlanguage-textoptionscodefield)。
