{

  "private": true,
  "name": "locationinsight-site",
  "scripts": {
    "clean": "rimraf dist",
    "sync": "npm run clean && wget -O gh-pages.zip  https://github.com/antvis/LarkMap/archive/refs/heads/gh-pages.zip && unzip gh-pages.zip && mv LarkMap-gh-pages dist",
    "build": "npm run sync"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "yuyanId": "180020010001227884",
  "author": "liupenghui.lph",
  "repository": {
    "type": "git",
    "url": "https://code.alipay.com/datavis/larkmap-site"
  }
}
