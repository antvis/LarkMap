import type { ReactFC } from '@formily/react';
import { connect } from '@formily/react';
import AntdSlider from './AntdSlider';

const Slider: ReactFC<any> = connect(AntdSlider);

export default Slider;
