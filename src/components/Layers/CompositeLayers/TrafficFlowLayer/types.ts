import type { TrafficFlowLayer, TrafficFlowLayerOptions } from '@antv/l7-composite-layers';
import type { LayerCommonProps } from '../../../../types';

/**
 * 组件类型定义
 */
export interface TrafficFlowLayerProps extends TrafficFlowLayerOptions, LayerCommonProps<TrafficFlowLayer> {}
