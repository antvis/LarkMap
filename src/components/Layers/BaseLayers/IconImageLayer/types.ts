import type { IconImageLayer, IconImageLayerOptions } from '@antv/l7-composite-layers';
import type { LayerCommonProps } from '../../../../types';

/**
 * 组件类型定义
 */
export interface IconImageLayerProps extends IconImageLayerOptions, LayerCommonProps<IconImageLayer> {}
