import type {
  BubbleLayerOptions,
  SourceOptions,
  ColorAttr,
  SizeAttr,
  TextLayerOptions,
} from '@antv/l7-composite-layers';

/**
 * 填充色
 */
export type SizeAttr1 = SizeAttr;

export { SourceOptions, ColorAttr, SizeAttr, TextLayerOptions };

/**
 * 组件类型定义
 */
export type BubbleLayerProps = BubbleLayerOptions;
